import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>Welcome to the Home Page</h1>
            <p>Navigate to Counter or About page using the links above.</p>
        </div>
    );
};

export default Home;
